<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.atactic.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!--
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    -->

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="io.atactic.android.element.AtacticApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_atactic_silverred"
        android:supportsRtl="true"
        android:theme="@style/AtacticTheme">
        <!-- android:roundIcon="@mipmap/ic_launcher_round" -->

        <activity
            android:name="io.atactic.android.activity.SimpleLoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AtacticTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AtacticTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->

        <activity
            android:name="io.atactic.android.activity.QuestListActivity"
            android:label="@string/title_activity_quest_list" />
        <activity
            android:name="io.atactic.android.activity.MapActivity"
            android:label="@string/title_activity_map" />
        <activity
            android:name="io.atactic.android.activity.QuestDetailActivity"
            android:label="@string/title_activity_quest_detail" />
        <activity
            android:name="io.atactic.android.activity.CheckInActivity"
            android:label="@string/title_activity_checkin" />
        <activity
            android:name="io.atactic.android.activity.ProfileActivity"
            android:label="@string/title_activity_profile" />
        <activity
            android:name="io.atactic.android.activity.RankingActivity"
            android:label="@string/title_activity_ranking" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>

</manifest>